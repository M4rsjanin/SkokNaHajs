# SkokNaHajs
A monorepo project for a fictional banking web application. The backend is powered by Spring Boot (Java) connecting to NeonDB (PostgreSQL) and AWS S3, while the frontend is built using React, TypeScript, and Vite.
